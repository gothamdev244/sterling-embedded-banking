# Sterling Platform - Embedded Banking Environment Configuration
# This file shows environment variables for configuring the embedded banking service

# CORS and Frame Ancestors Configuration
# Comma-separated list of allowed origins for CORS and iframe embedding
VITE_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:9000,http://localhost:3000,http://localhost:3001

# For ECS/Production deployment, update with actual service URLs:
# VITE_ALLOWED_ORIGINS=https://agent-frontend.example.com,https://main-app.example.com

# Service URLs (if needed for direct API calls)
# These are typically proxied through the agent frontend, but can be configured for direct access
VITE_AI_SERVICE_URL=http://127.0.0.1:8000
VITE_SEARCH_SERVICE_URL=http://127.0.0.1:8091  
VITE_WEBSOCKET_URL=ws://127.0.0.1:8080